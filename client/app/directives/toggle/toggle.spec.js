'use strict';

describe('toggleDirective', function () {

    // load the service's module
    beforeEach(module('toggleDirective'));

    // instantiate service
    var toggleService, scope, createController;
    beforeEach(inject(function (_toggleService_) {
        toggleService = _toggleService_;
    }));

});

